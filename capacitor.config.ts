import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.austinhunter.balanced',
  appName: 'Budget Wisely',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
